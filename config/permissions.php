<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Menu Permissions
    |--------------------------------------------------------------------------
    |
    | 메뉴 권한 시스템에서 사용되는 권한 설정입니다.
    | 각 권한은 비트마스크로 표현되며, 최대 8개의 권한을 지원합니다 (2^8 = 255).
    |
    */

    'menu' => [
        1 => 'Index',           // 2^0 - 색인/목록 권한
        2 => 'Read',            // 2^1 - 읽기 권한
        4 => 'CmtRead',         // 2^2 - 댓글 읽기 권한
        8 => 'CmtWrite',        // 2^3 - 댓글 쓰기 권한
        16 => 'CmtFileUpload',  // 2^4 - 댓글 파일 업로드 권한
        32 => 'Write',          // 2^5 - 쓰기 권한
        64 => 'FileUpload',     // 2^6 - 파일 업로드 권한
        128 => 'FullControl',   // 2^7 - 완전 제어 권한
    ],

    /*
    |--------------------------------------------------------------------------
    | Permission Values
    |--------------------------------------------------------------------------
    |
    | 특정 권한 조합의 값들입니다.
    |
    */

    'values' => [
        'all' => 255,           // 모든 권한 (1+2+4+8+16+32+64+128)
        'basic' => 3,           // 기본 권한 (Index + Read)
        'comment' => 28,        // 댓글 관련 권한 (CmtRead + CmtWrite + CmtFileUpload)
        'upload' => 80,         // 업로드 관련 권한 (FileUpload + CmtFileUpload)
    ],
];
